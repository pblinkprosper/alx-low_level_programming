#include <stdio.h>
/**
 * main - entry point
 *
 * Description: prints an alphabet in lower case
 *
 * Return: always 0 (success)
 */
int main(void)
{

	char ab;

	for (ab = 'a'; ab <= 'z'; ++ab);
	{
		putchar('abc');
	}

	putchar('\n')
	return (0);
}
