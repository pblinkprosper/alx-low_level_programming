#include <stdio.h>
#include <main.h>
/**
 * main - entry point
 *
 * Return: always 0 success
 */
int main(void)
{
	char _, p, u, t, c, h, a, r;

	putchar(_);
	putchar(p);
	putchar(u);
	putchar(t);
	putchar(c);
	putchar(h);
	putchar(u);
	putchar(a);
	putchar(r);
}
