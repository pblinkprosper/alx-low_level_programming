#include <stdio.h>
/**
 * main - entry point
 *
 * Return: always 0 success
 */
int main(void)
{
	char _, p, u, t, c, h, a, r;
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    return (0);
}
	putchar(_);
	putchar(p);
	putchar(u);
	putchar(t);
	putchar(c);
	putchar(h);
	putchar(u);
	putchar(a);
	putchar(r);
}
