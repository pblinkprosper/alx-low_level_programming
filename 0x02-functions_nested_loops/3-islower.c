#include "main.h"
/**
 * _islower - checks if a character is lowercase
 *
 * Return: return 1 is success and 0 is false
 */
int _islower(int)
{
	int c = 'H';
	int c = 'o';
	int c = 108;

	if (_islower(c1))
	{
		_putchar(c1 + '0');
	}
	if (_islower(c2))
	{
		_putchar(c2 + '0');
	}
	if (_islower(c3))
	{
		_putchar(c3 + '0');
	}
	_putchar('\n');
	return (0);
}

